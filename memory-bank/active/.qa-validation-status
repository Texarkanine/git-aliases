PASS

QA review of git-sync-worktrees build phase.

Findings:
- KISS: diff is minimal — checkout+pull+checkout-back replaced by a single upstream-resolve + fetch + onto-ref block. No new abstractions, functions, or flags introduced.
- DRY: no duplicated logic; existing stash/confirm/error-restore blocks untouched and reused as-is.
- YAGNI: no speculative code. `HAS_REMOTE` retained only for its original no-remote warning message, not dead code.
- Completeness: all 4 acceptance criteria and all 9 planned test behaviors are implemented and covered; no stubs or TODOs found in the diff.
- Regression: naming, indentation, and error-handling style match the surrounding script; stash-safety contract (never pop a pre-existing stash) is unchanged and re-verified by test_stash_safety.
- Integrity: no hardcoded shortcuts, magic numbers, or debug artifacts; fetch failure path correctly restores stash before exiting, matching the removed checkout-failure path's behavior.
- Documentation: subcommands/git-sync/README.md workflow section rewritten to match the new fetch/onto-ref behavior; no other docs reference the old checkout-based workflow.

Verification re-run: `make test` (all suites incl. `tests/test-git-sync.sh`, 9/9 cases) and `make shellcheck` both pass clean on the current tree.

Advisory (non-blocking): `scripts/run-shellcheck.sh` only scans `*.sh`, so `git-sync.bash` itself is never linted by `make shellcheck`. Pre-existing repo-wide pattern (documented in systemPatterns.md Bash vs. POSIX split), not introduced by this task — no action required here.
