FAIL (fixable)

# Preflight Findings — git-wt

## TDD Plan Encoding — PASS

Each executable unit (1: git-wt CLI, 2: shell wrappers, 3: install script) is ordered stub-tests → stub-interface → write-tests-and-run-red → write-code-and-run-green, matching the always-tdd.mdc four-step process exactly. Unit 4 (README docs) correctly claims the prose/policy exemption and carries no test steps. No scheduled change-detectors found among the new test cases: Unit 3's "`all` does not depend on `shell`" / "`clean` invokes the shell uninstaller" checks assert on actual `make -n all` / `make -n clean` dependency-resolution behavior (a real, user-facing contract — default `make` must never touch RC files) rather than on document prose, so they are not change-detectors and were left in place.

## Convention Compliance — PASS (one advisory below)

`subcommands/git-wt/git-wt.bash` matches the existing `git-sync.bash` / `git-identity.bash` layout and naming (install-subcommands.sh strips the `git-` prefix and reinstalls `git-<name>`). The new top-level `shell/` directory and `scripts/install-shell-integration.sh` match both the issue's specified repo layout and the existing `scripts/install-*.sh` fence-block pattern (`install-completions.sh`). New test files (`tests/test-git-wt.sh`, `tests/test-wt-wrappers.sh`, `tests/test-install-shell-integration.sh`) match the existing POSIX `tests/*.sh` + `make test` convention (`test-trim.sh`: `#!/bin/sh`, `set -eu`, failure counter). Deferring `git-wt-completion.bash` is consistent with systemPatterns.md's "adding a subcommand does not automatically gain completion" note.

## Dependency Impact / Completeness Precheck — FAIL (fixable)

Component Analysis states "Makefile: ... `test` must run the new test files" (plural — all three). Implementation Plan Units 1 and 2 each carry an explicit instruction to wire their new test file into the Makefile's `test` target ("Wire `Makefile` `test` target to run `test-git-wt.sh`" / "Wire `make test` to run this file"). Unit 3 step 4 has no equivalent instruction for `tests/test-install-shell-integration.sh` — it only says "Run tests then `make test`," which describes verifying the plan, not updating the `test` recipe. As written, an implementer can satisfy step 4 by running the test file once by hand and calling the unit done, leaving it permanently unwired from `make test` and silently dropping coverage for the installer/Makefile-contract tests thereafter.

**Fix:** add an explicit instruction to Unit 3 step 4 — e.g. "wire `tests/test-install-shell-integration.sh` into the Makefile `test` target" — matching the phrasing already used in Units 1 and 2.

## Conflict Detection — PASS

No existing `worktree`, `wt-*`, or `shell/`-directory code exists in the repo outside the memory-bank planning docs themselves; this is a clean addition with no overlapping implementation to reconcile.

## Advisories (non-blocking)

1. **Stale documentation on install-script POSIX-ness:** `memory-bank/systemPatterns.md` ("Shell code in Makefile and install scripts: POSIX `/bin/sh` only — no bashisms") and `memory-bank/techContext.md` ("POSIX-compatible install scripts") both claim `scripts/install-*.sh` are POSIX, but the actual `install-completions.sh` and `install-subcommands.sh` are `#!/usr/bin/env bash` and use bashisms (`[[ ]]`). The plan correctly follows the real codebase (Bash installer) rather than the stale doc claim, and Component Analysis explicitly calls this out. Recommend a surgical correction to `systemPatterns.md`/`techContext.md` in a future task (out of scope for this plan; the workspace rule scoping POSIX to "Makefiles and git aliases" specifically, not install scripts, already matches reality — only the memory-bank docs are wrong).
2. **Radical Innovation:** add a `git wt list [--porcelain]` subcommand now, built on the same `wt_owner_repo`/`wt_worktree_path` helpers already planned for `go`/`done`. It gives users a discovery command for free, and — more usefully — turns the deferred `git-wt-completion.bash` into a thin wrapper over `git wt list --porcelain` instead of a separate scrape of `git worktree list --porcelain`, so the "completion is a stretch/follow-up" gap mostly closes itself whenever someone picks it up. Not applied to the plan; operator's call whether to fold it in now or leave it for the completion follow-up.
