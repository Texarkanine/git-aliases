PASS WITH ADVISORY

Wrong-reference: progress.md Complexity is Level 2. This is the correct check file.

Prerequisites: tasks.md marks plan, TDD, technology validation, and pre-mortem complete. No creative docs are required at Level 2.

TDD plan encoding: Units 1–3 are executable and each numbers stub tests (including a `run_one` line in `main()`), stub interface, write tests (red), then write code (green). Units 4–5 are prose/policy (README / `usage()`, a GitHub issue) and correctly omit tests. No scheduled change-detectors. No step swap or strike.

Convention: files and `wt_*` / `cmd_*` names match `subcommands/git-wt/git-wt.bash` and `tests/test-git-wt.sh`. `sorted_lines`, `run_with_timeout`, isolated `HOME`, and `run_one` already exist. Wrappers already forward leftover args, so `wt cleanup +cursor --list` needs no wrapper logic change. There is no `git-wt` completion script.

Prior FAIL (fixable) items are closed in this revision. Unit 2 resolves scanned Cursor roots with `wt_main_worktree`, skips unusable roots the way `wt_all_mains` does, uniques the combined main list, and unit 3 adds `test_cleanup_all_cursor_yes_once`. Units 1–3 each require `run_one` registration. Unit 1 prints HOME-logical Cursor paths and matches logical and physical porcelain forms. Unit 3 matches the session directory the same way. Unit 4 updates `usage()` and the file-header usage block.

Issue #11 is covered: `+cursor` anywhere, same `--list` / confirm / `--yes` / `--force` / dirty-skip rules, membership is a linked worktree under `~/.cursor/worktrees` (detached included), empty session leftovers ignored, `--all` discovers repos by scanning that directory, empty `~/.cursor/worktrees/<name>/` removed after a successful remove, unknown `+source` is an error. `+claude` stays an issue.

Advisory — fold the exclusion bullet into the current-repo list test. Behaviors to Verify lists three current-repo listing cases (include go + Cursor including detached; token position; exclude another repo's Cursor worktree, a plain `git worktree add`, and main). Unit 1 names five tests and says "assertions for the five behaviors above," so `test_cleanup_list_cursor_current_repo` is the only place those exclusions fit. Write them there, the way `test_cleanup_list_current_repo` already excludes foreign and main. A test that only asserts inclusion will not catch a filesystem-scan membership bug.

Advisory — wrapper help stays silent on `+cursor`. Unit 4 updates `git-wt.bash` `usage()` / header and the README. `shell/wt.bash` and `shell/wt.zsh` have their own usage heredocs (`cleanup [--all] [--list] [--yes] [--force]`). `wt help` will not mention `+cursor` unless those two strings are updated. Logic needs no change. Also update the README cleanup mermaid if the prose changes: it still says `--all` is only repos under `~/worktrees` and only go-layout paths.

Advisory — radical innovation (does not block). Do not add a third parallel trio (`wt_claude_worktrees` / `wt_all_claude_mains` / an after-remove hook) when `+claude` lands. Collapse membership into source descriptors so `+cursor` is data, not a second function family:

- `go`: root `${HOME}/worktrees`, path must be the layout path for the checked-out branch (today's `wt_created_worktrees`).
- `cursor` (if `+cursor`): root `${HOME}/.cursor/worktrees`, any linked worktree under that root, include detached, after a successful remove `rmdir` the empty session directory.

One helper lists worktrees for a source (porcelain filter). One helper scans a root, resolves mains, uniques. `cmd_cleanup` enables sources from flags, uniques mains from every enabled root, unions paths from every enabled source. `--all` is "scan each enabled root," not a Cursor-only discovery function. The `+claude` issue can then name a third descriptor (`<repo>/.claude/worktrees/`, default only) instead of another scan/list/rmdir trio. Do not make this change in the current plan.
