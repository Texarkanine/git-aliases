FAIL (fixable)

TDD Plan Encoding: PASS. Units 1-3 each order test stubs, interface stubs, implemented tests with a red run, then production code and a green run, explicitly enough that an implementer cannot code first. Units 4 and 5 are prose/policy (README, persistent memory-bank) and correctly carry no tests. No scheduled change-detectors: the two `make -n` behaviors assert a build-system contract that goes red when `make test` silently stops running a suite or when `make completions` invokes a script that no longer exists, not merely when a document is edited. They also match the existing precedent in `tests/test-install-shell-integration.sh`. No in-phase plan edits were made.

Convention compliance: PASS on both prior failures. Unit 3 renames `scripts/install-completions.sh` to `scripts/install-completions.bash` via `git mv` and updates the Makefile callers, satisfying the filename/shebang rule without a POSIX rewrite and without dragging in the other misnamed `scripts/install-*.sh`. Unit 5 records the RC-file split in `systemPatterns.md`, `productContext.md`, and `techContext.md`. The new `.zsh` completers and `.sh` POSIX test suites both match the documented dialect-by-filename rule.

Completeness: FAIL (fixable). The behavior "install without zsh: `PATH` without `zsh` -> no zshrc fence; bash copy and `~/.bash_completion` fence still happen" has no viable implementation path in the plan. The installer needs `find`, `tr`, `mkdir`, `touch`, `cp`, `chmod`, `basename`, `awk`, and `mv` from `PATH`, and `zsh` shares a directory with several of them on both target platforms (`/bin/zsh` with `/bin/cp` on macOS, `/usr/bin/zsh` with `/usr/bin/awk` on the `ubuntu-latest` CI runner). No directory can be dropped from `PATH` to hide `zsh` while leaving the installer runnable, so the naive test makes the installer crash and then passes for the wrong reason: no fence because nothing ran. Name the mechanism in the plan before build. Two workable options: (a) have the installer resolve zsh through an overridable variable, e.g. `GIT_ALIASES_ZSH="${GIT_ALIASES_ZSH-$(command -v zsh || true)}"`, and have the test set it empty; or (b) have the test build a temp `PATH` directory of symlinks to the utilities the installer needs, deliberately excluding `zsh`. Option (a) is smaller and cannot false-pass. Whichever is chosen, the test must also assert the installer exited 0 and that the bash copy plus `~/.bash_completion` fence are present, so a crash cannot read as a pass.

Convention compliance: advisory. Renaming the installer to `.bash` silently drops it from ShellCheck coverage, since `scripts/run-shellcheck.sh` scans tracked `*.sh` only. The plan names this tradeoff and declines to widen ShellCheck in this task, which is consistent with `install-shell-integration.bash`. `tests/test-shellcheck.sh` asserts nothing about the file list, so nothing breaks. Recording it so the loss of coverage is a decision and not a surprise.

Dependency impact: all four `Makefile` references to the installer (two in `completions`, two in the `clean` chmod and uninstall lines) are covered, and `test_make_completions_and_clean_invoke_bash_installer` catches any straggler because `install-completions.bash` does not contain the substring `install-completions.sh`. No test or workflow file references the installer today. README sections 9, 25, and 67-77 are covered by unit 4. Two memory-bank occurrences beyond the ones the plan enumerates should be swept while those files are open: `systemPatterns.md` line 25 lists `install-completions.sh` in the idempotency pattern, and `productContext.md` line 12 describes the completion use case as bash-only. Neither needs a plan change; note them in the unit 5 steps.

Dependency impact: `tests/test-install-completions.sh` is the first test in this repository to run a real installer that writes an RC file. It must use the `run_isolated` pattern from `tests/test-install-shell-integration.sh` for every case that invokes the installer, including the without-zsh case, or a test run will edit the operator's `~/.zshrc`. The plan says to reuse the pattern; make it non-negotiable in build.

Conflict detection: no existing `.zsh` completer duplicates the proposed work; `shell/wt.zsh` is the opt-in `make shell` wrapper and does not overlap. The distinct-fence requirement is correct and tested. The installer's existing `if [ -f ... ]` guard inside the `"$INSTALL_DIR"/*-completion.bash` loop must be mirrored for the `.zsh` glob, because the script runs under `set -u` without `nullglob` and an unmatched glob would otherwise be treated as a filename.

Completeness: acceptance criterion 1 ("zsh tab-completes `git sync`") is deliberately not covered end to end. The plan tests the completer functions directly and does not drive `git <TAB>` through zsh's `_git`, with the dispatcher mismatch documented in Challenges and the residual risk accepted in the Pre-Mortem. That is a reasonable scope call, but it means AC1 rests on an operator smoke test. QA should perform that smoke test explicitly rather than reading a green suite as AC1 satisfied.

Advisory - red run informativeness: unit 3 step 2 stubs the new functions in `scripts/install-completions.sh` while the test suite already points at `scripts/install-completions.bash`, and the `git mv` does not happen until step 4. Every case in the suite therefore goes red for a missing-file reason, including `test_bash_copy_and_fence_unregressed`, so the red run proves nothing about behavior. Move the `git mv` and the Makefile caller update into step 2 alongside the stubs. The red run then fails on the zsh assertions only, which is the signal TDD is supposed to produce.

Advisory - radical innovation: give both dialects one source of truth for candidates instead of two hand-mirrored completers. Have each subcommand answer its own completion query - `git-sync --complete "<prev>" "<cur>"` printing one candidate per line, guarded so it never runs as a normal invocation - and reduce both `git-sync-completion.bash` and `git-sync-completion.zsh` to a thin adapter that shells out and feeds the result to `compgen -W` or `compadd` respectively. Adding a flag or a branch name then changes one file, the shell-agnostic one, and the "lockstep drift with bash" challenge disappears rather than being mitigated. The cost is one process per completion and a new argument contract on each subcommand, and it would touch `git-sync.bash` and `git-identity.bash`, which this task otherwise leaves alone. Evaluate against the cost of that redesign; it is not required to satisfy the brief, and the current plan is correct without it.
