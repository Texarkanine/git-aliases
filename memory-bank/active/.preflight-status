PASS WITH ADVISORY

Level 2 plan checked against `subcommands/git-wt/git-wt.bash`, `shell/wt.bash`, `shell/wt.zsh`, `tests/test-git-wt.sh`, and `tests/test-wt-wrappers.sh`. Every executable unit (steps 1-4) orders stub tests, stub interface, red tests, then code. Step 5 is prose and correctly owes no tests. File locations and dialects follow `systemPatterns.md`: the subcommand is `.bash`, the tests are POSIX `.sh`, and the `${@:2}` wrappers are `.bash`/`.zsh`. `git wt` has no completion scripts, so there is no companion to update. Every brief requirement maps to a concrete step. The plan's shared `wt_remove_worktree` helper stands in for calling `git wt done` per worktree, which is equivalent and avoids spawning a process per worktree.

In-phase plan edit (strike): the "help lists cleanup" behavior and the `test_help_flags` gains a `cleanup` assertion clause in step 2.1 are struck as change-detectors. That assertion can only go red when someone edits the `usage()` heredoc it checks. Dispatch of `-h` is already covered by the existing go/done assertions. The `usage` text update itself stays in the plan.

Advisory findings:

- Wrapper partial failure leaves the shell in a deleted directory (medium). Step 4 says to capture stdout and `cd` "like done". But `done`'s wrapper does `dest="$(...)" || return $?`. Step 3 has `git wt cleanup` print the main path and still exit 1 when another row failed. With `|| return`, the wrapper drops that path and the user's shell stays in the removed worktree. Fix: capture the status, `cd "${dest}"` when dest is non-empty, then return the status. Add a mock-git wrapper test where cleanup prints a path and exits 1.
- A missing layout directory aborts discovery under `set -e` (medium). `wt_created_worktrees` compares `pwd -P` of both paths. For a foreign worktree the layout path usually does not exist, so `x="$(cd missing && pwd -P)"` kills the script. `test_cleanup_list_current_repo` will catch this. Porcelain entries marked `prunable` (the worktree directory was deleted by hand) fail the same way and are not tested. Skip entries whose path or layout path does not exist, or skip `prunable` entries.
- The parent's cwd can vanish in the middle of the loop (low). Step 3 runs each removal in a `( cd main && ... )` subshell, which does not move the parent. After a row removes the worktree that holds cwd, a later row's `pwd -P` in the parent fails. Compute `pwd -P` once before the loop, or `cd` the parent to that row's main. The plan's "cd to that row's main" wording is ambiguous about which process moves.
- Usage text for `done --yes` (low). Step 1 does not say to update `usage()` or the header comment block at the top of `git-wt.bash` (lines 9-14 repeat the usage text). Step 2 covers `cleanup` in `usage` only. Update both places for both flags.
- Locked worktrees (low). `git worktree remove` refuses a locked worktree even with a single `--force`, so cleanup will count it as a failed row and exit 1. That is acceptable. Say so in the README next to the dirty/skip rules.
- `wt_scan_worktree_roots` walks stray directories (low). A directory under `~/worktrees/<owner>/<repo>/` with no `.git` anywhere below it is walked all the way down. Only descend into directories whose name starts with `<repo>-` at the first level, which is still slash-branch aware.
- `wt_owner_repo` runs for every branch (low). `wt_worktree_path` resolves the main checkout and the remote again for each porcelain entry. Compute the `~/worktrees/<owner>/<repo>/<repo>-` prefix once per main.

Radical innovation (advisory, does not block): have `go` register the repo so `cleanup --all` does not have to scan the filesystem. `cmd_go` appends the main checkout path to `~/.local/state/git-wt/repos` (deduped). `cleanup --all` reads that file, skips missing or non-repo entries, and runs the per-repo discovery unchanged. This removes `wt_scan_worktree_roots`, its slash-branch recursion, the orphan-root warnings, and the risk of walking stray directories. It also finds repos whose owner/repo mapping changed since the worktree was created. Keep the scan as a one-time fallback for worktrees made before the registry existed, or drop it and document that. Cost: one new state file and a write in `go`, plus a test for the registry.
